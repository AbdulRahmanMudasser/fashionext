enum QueryType {
  all,
  popular,
  unisex,
  men,
  women,
  kids
}

enum WishlistToggle {
  retrieve,
  delete,
  add,
}

enum FetchOrdersTypes {
  pending,
  delivered,
  cancelled,
}

enum ReloadCount {
  delete,
  reload,
  update,
}

enum NotificationCount {
  update,
  reload,
}